name: LunarHost Ephemeral Node

on:
  workflow_dispatch:
    inputs:
      username:
        description: 'Username of the server to launch'
        required: true
        default: 'gamer123'
  repository_dispatch:
    types: [renew_server]

jobs:
  run-server:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code Engine (mc-launch)
        uses: actions/checkout@v4

      - name: Checkout Database & World (mc-disk)
        uses: actions/checkout@v4
        with:
          repository: AADI-playz23/mc-disk # The private storage repo
          token: ${{ secrets.LUNAR_DB_TOKEN }}
          path: mc-disk # The folder name it will be downloaded into

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Run LunarHost Core
        env:
          USERNAME: ${{ github.event.inputs.username || github.event.client_payload.username }}
        run: |
          chmod +x launch.sh
          ./launch.sh

      - name: Trigger Next Cycle
        if: always()
        run: |
          curl -X POST https://api.github.com/repos/${{ github.repository }}/dispatches \
          -H "Accept: application/vnd.github.v3+json" \
          -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
          -d '{"event_type": "renew_server", "client_payload": {"username": "${{ github.event.inputs.username || github.event.client_payload.username }}"}}'
